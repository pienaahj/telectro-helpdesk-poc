# compose.local.yaml
#
# Deprecated local override.
#
# Local defaults now live in compose.override.yaml so plain:
#
#   docker compose up -d
#
# works as expected.
#
# This file is kept temporarily to avoid breaking old helper commands.

services: {}
